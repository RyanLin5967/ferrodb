WINDOWS STATUS_ACCESS_VIOLATION — raw evidence, 2026-09-18
==========================================================
Provenance: every line below is [HERE] — read by this session from the CI logs and from
the tree. Nothing in this file is second-hand.

THE OBSERVATION
---------------
run 35391545783, head 0ee07fc, job "Test windows-latest":

  process didn't exit successfully:
  `D:\a\ferrodb\ferrodb\target\debug\deps\integration_cluster_agents-a50a5c1993fcbae7.exe`
  (exit code: 0xc0000005, STATUS_ACCESS_VIOLATION)

ubuntu-latest and macos-latest both passed on the same commit.

⛔ CORRECTION TO THIS SESSION'S OWN EARLIER RECORD
--------------------------------------------------
I recorded "all 35 tests print ok, then the process dies at teardown". That is FALSE, and
it sent three hypotheses (a use-after-free in the mirror, a production drain thread in the
touch queue, unjoined fleet threads) at the wrong window.

  binary declared:            running 35 tests
  lines reporting "... ok":   34
  summary line ("test result: ok. N passed"):  ABSENT

The process died BEFORE libtest printed its summary and with one test unreported. Diffing
the 34 reported names against the 35 `#[test]` fns at 0ee07fc names it exactly:

  a_hundred_agent_writes_across_three_branches_leave_only_forks_and_merges_in_the_log

That test is the heaviest in the file: 3 branches x 100 UPDATEs = 300 statements, each
followed by fleet.pump_all() over a real 3-node TCP cluster, then 3 merges.

So the fault is INSIDE a named test, not at teardown. Timeline from the log:
  20:53:15.6657Z  last "... ok"   (on_three_real_nodes_only_the_merge_reaches_a_quorum)
  20:53:17.5133Z  abnormal exit   (1.85 s later, no summary line)

WHAT THIS RULES OUT
-------------------
src/buffer/ contains ZERO occurrences of `unsafe`:
  grep -rn 'unsafe' src/buffer/ | wc -l   ->   0
The compiled delta from the last windows-green (8aeae20) to the red (0ee07fc) is the D35-C1
+ D44 buffer work: page_table.rs (243 non-comment lines), touch_queue.rs (159),
buffer_pool.rs (97), arena.rs (2), plus three test files. None of it contains unsafe, so
memory corruption originating in that delta is not a available mechanism. What remains for
safe Rust to reach 0xc0000005 is stack exhaustion or a libstd/OS interaction.

The one thread_local! in the delta (touch_queue.rs:56) is `Cell<usize>`, which has no
destructor, so the TLS-destructor route is closed too.

SECOND SAMPLE: DESTROYED BY MY OWN PUSH
---------------------------------------
run 35394190730 on 5bb234b reached windows=in_progress and then CANCELLED. Cause, read from
the tree: .github/workflows/tests.yml:20-22

  concurrency:
    group: ci-${{ github.event_name }}-${{ github.ref }}
    cancel-in-progress: true

I pushed 0aaffb4 at 21:16Z while the 20:57Z run was still in its windows job. The push
cancelled it. This is the CI form of "never write into a tree while a run against it is in
flight": a sample in flight is destroyed by the next push to the same ref.

THIRD SAMPLE: GREEN
-------------------
run 35395893959, head 0aaffb4 (a strict superset of 0ee07fc):
  success Test ubuntu-latest
  success Test macos-latest
  success Test windows-latest

⚠ The asymmetry recorded before the sample still holds and must not be forgotten: a PASS is
the LESS informative outcome here, because an intermittent fault passes most of the time.
One green does not retire one red.

LOCAL STRESS, macOS, 40 ITERATIONS: CLEAN
-----------------------------------------
The exact test, alone, 40 times, --test-threads=1:
  40 iterations, 0 non-zero exits
Collection verified rather than assumed — the per-iteration output reads
  running 1 test
  test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 34 filtered out; finished in 1.86s
and the zero-collected trap was forced on purpose: `--exact no_such_test_exists` prints
"running 0 tests" and still exits 0, so an rc-only check would have reported 40 clean runs
of nothing.

VERDICT
-------
UNRESOLVED. One occurrence, ever. Not reproduced in 40 local runs. The tip is green on all
three platforms. The mechanism is unknown and the two D44-shaped explanations are closed by
`unsafe`=0.

The operational conclusion does not depend on the diagnosis: an unexplained access
violation inside a test is not something to merge into main, because main's CI going
intermittently red destroys the signal every other lane reads.


=== 2026-09-20 — THE SAME TEST FAILED ON WINDOWS AGAIN, THIS TIME WITH A DIAGNOSABLE CAUSE ======
⛔ THIS DOES NOT EXPLAIN THE ACCESS VIOLATION. A panic is not a 0xc0000005 and no mechanism links
them. What it adds is the first REPRODUCIBLE Windows-only failure this test has ever produced.

CI run 35499141131, windows-latest, head cc399de (D64c). ubuntu and macos both SUCCESS.
  test a_hundred_agent_writes_across_three_branches_leave_only_forks_and_merges_in_the_log FAILED
  test result: FAILED. 34 passed; 1 failed.
  panicked at tests\integration_cluster_agents.rs:1467:72:
  "branch 3: not the leader, and this node does not currently know who is - an election is in
   progress or this node is partitioned from the cluster. Retry shortly; do not treat this as the
   leader being down."

WHAT IS NOW KNOWN, and it is worth more than the diagnosis:
  · The SAME TEST, and the SAME 34-passed/1-failed SPLIT as the 2026-09-18 access violation. That
    earlier run died before libtest printed its summary with exactly one test unreported, and the
    diff of reported names identified THIS test. Two Windows failures, one test.
  · The mechanism here is timing: `hold_leader` establishes leadership and waits for every replica
    to agree, but NOTHING PUMPS HEARTBEATS between that call and the merge four lines later, so on
    a slow runner the lease lapses and a follower starts an election in the gap.
  · So this test is TIMING-SENSITIVE ON WINDOWS in a way it is not on the other two platforms —
    which is the property a hunt for the access violation should start from. It is the heaviest
    test in the file (3 branches x 100 UPDATEs, each followed by pump_all over a real 3-node TCP
    cluster, then 3 merges), and Windows is where the wall-clock gaps are longest.

FIXED by a bounded retry on `NotLeader` ONLY (a7f40e2), fire-checked by forcing the first attempt
to return it. Any other error still fails immediately.

⚠ THE OPERATIONAL CONCLUSION IS UNCHANGED: agent-isolation still must not merge to main. The
access violation remains UNEXPLAINED, one occurrence, never reproduced. Fixing a timing flake in
the same test does not retire it — and if anything this makes the case stronger that the test is
where to look, not weaker.
================================================================================================

================================================================================
⛔⛔ 2026-09-22T20:39Z — THE D138 BISECT'S PREMISE IS FALSIFIED BY THIS FILE'S OWN
     EARLIER ENTRIES. THE CONTROL ARM IS NOT A CONTROL.
================================================================================

The bisect drew b17ec58 (pre-D138) against 4685d80 and read 0-red-of-3 vs 2-red-of-3
as suggestive that D138 introduced the access violation.

⇒ EVERY EARLIER OCCURRENCE RECORDED ABOVE IS AN ANCESTOR OF b17ec58. Tested, not
  assumed, with `git merge-base --is-ancestor <sha> b17ec58`:

    0ee07fc   run 35391545783   ancestor-of-b17ec58 = YES
    0aaffb4   run 35395893959   ancestor-of-b17ec58 = YES
    cc399de   run 35499141131   ancestor-of-b17ec58 = YES

⇒ The "clean" arm CONTAINS three prior access violations. A green b17ec58 therefore
  exonerates nothing: it is an intermittent fault that did not happen to fire in three
  draws of an arm whose own history holds three firings.

⇒ THE CORRECT STATEMENT, replacing the bisect's: the access violation is a
  LONG-STANDING INTERMITTENT, present since at least 0ee07fc, observed on FOUR distinct
  shas (0ee07fc, 0aaffb4, cc399de, 4685d80), and the 0/3-vs-2/3 split is consistent
  with a constant background rate. One-sided Fisher exact p ≈ 0.20 — and the third draw
  MADE IT WEAKER (0.17 → 0.20) because it came up green.

⚠ THE READING THAT WAS RETRACTED ALONG THE WAY: "RED AND REPRODUCIBLE, 2 OF 2 on
  4685d80" is wrong. Read per-attempt rather than run-level, that run is
  FAIL · FAIL · SUCCESS = 2 of 3, and run-level colour follows the LATEST attempt, so
  origin/main reads GREEN while two of its three Windows attempts crashed.

⚠ AND THE ARMS WERE NOT COMPARABLE ANYWAY: 4685d80's run is event=push on main;
  b17ec58's is event=pull_request on d81-land. Almost certainly causally irrelevant to
  an access violation, but it is a difference between arms and it was never stated.

⇒ ACTION TAKEN: both draw-4 runs CANCELLED at 20:39Z. They could no longer
  discriminate, and they were the only thing holding the batch push — the CI
  concurrency group (`ci-${event_name}-${ref}`, cancel-in-progress: true) means a push
  to main cancels an in-flight push-run on main. Cancelling converts a dead statistical
  instrument into an immediate DIRECT one:

⭐ THE REPLACEMENT INSTRUMENT IS DIRECT, NOT STATISTICAL. `av-loop-counters` prints an
  ITERATION COUNT out of both bounded loops in the crashing file, and it is in the push
  batch. Its macOS baseline, measured across 16 loop exits in a full verifier run:

      max turns = 3        (bounds are 20 000 and 100 000)
      max ms    = 10

  Four orders of magnitude of headroom, so a Windows line in the thousands is
  unmissable. Reading, pre-registered here BEFORE the Windows run:
      turns near the bound      -> the ELECTION is not converging
      turns small but ms huge   -> the SOCKET LAYER (transport.rs:944 already records
                                   that WouldBlock and TimedOut are the same event on
                                   different platforms)
      no LOOPCOUNT line at all  -> it died before either loop, which is the AV itself

⚠ The instrument only reports because `eprintln!` was replaced: libtest's output capture
  swallows it under the plain `cargo test` CI runs, and the AV kills the process so
  captured buffers never flush. Two-armed fire-check: 0 lines before, 2 after.
