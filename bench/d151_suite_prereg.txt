D151 — SUITE PRE-REGISTRATION. Written before `tools/verify-suite.sh` was invoked.

MODE:     per-target (VERIFY_MODE=per-target). Quoted with the mode, because `whole` and
          `per-target` are not the same number: `whole` = `per-target` + 2 doctests, and mixing
          them manufactures a phantom regression. This file's number is a PER-TARGET number.

BASE:     `main` tip 2a0a8fc, whose bank (bench/land-2655e3b_certified) records
          `suite per-target rc=0 passed=2508 failed=0 head=b17ec58`.

DIFF:     bench/ text plus src/pgwire/extended.rs and src/pgwire/mod.rs. No file under tests/ is
          touched, no target is added or removed.

TESTS ADDED BY THIS BRANCH: ZERO. Counted from what RUNS, not from the diff text:
  - `git diff 2a0a8fc..d151-parse-site -- src tests examples | grep -cE '^\+[[:space:]]*#\[(test|tokio::test)\]'`
    returns 0. `[[:space:]]`, not `\s` — `git grep -E` does not honour `\s` on this platform, and
    a pattern that silently matches nothing returns a zero that reads exactly like a real zero.
  - The diff adds no `macro_rules!` body either, so the `reaper_suite` trap (one `#[test]` inside
    that macro compiles to TWO tests, once per invocation) cannot apply: a count taken from the
    diff would be wrong in the same direction whether or not I had noticed it, and here there is
    no macro to be wrong about.

PRE-REGISTERED RESULT:  rc=0, passed=2508, failed=0, per-target.

FALSIFIERS
  - passed != 2508: a test appeared or vanished that this diff does not explain. Name it before
    quoting any total.
  - failed > 0 or rc != 0: the row does not land, whatever the announcement count says.
  - passed = 0, or any target collecting nothing: a run that collected nothing has not passed.
  - a whole-mode number compared against this one: refuse it; they are different quantities.

A SUITE VERDICT IS NOT A LANDING GATE. `tools/land-gate.sh` asks three questions — certify-head,
staleness, prepush — and the suite is one input to the first. All three are run and recorded.

-----------------------------------------------------------------------------------------------
AMENDMENT 1 — the branch now adds ONE test, so the pre-registered total changes. Written before
the suite was run; the earlier launch was stopped while still QUEUED behind d138c's suite and
never acquired the lock, so no suite number has been taken on this branch at all.

WHAT WAS ADDED AND WHY
  tests/d151_parse_reads_a_refreshed_snapshot.rs — one `#[test]`, in a NEW target.

  D151 makes parse-time correctness depend on `Catalog::epoch` for the first time: before it,
  `parse_one` held the live catalog and could not see a stale schema. `d54_as_of_in_session`
  already covers same-connection DDL-then-parse; the case D151 introduces — a connection that
  CACHED BEFORE another connection's DDL — was reached by nothing in the suite.

COUNTED FROM WHAT RUNS, NOT FROM THE DIFF
  One `#[test]` attribute, no `macro_rules!` body, so the `reaper_suite` doubling trap does not
  apply. `cargo test --test d151_parse_reads_a_refreshed_snapshot` reports
  `1 passed; 0 failed`, which is the count this amendment uses — the target was run, not read.

THE TEST WAS FIRE-CHECKED, and the mutation and its output are banked in
bench/d151_firecheck.txt. A green test on fixed code is not evidence the test can see the defect.

PRE-REGISTERED RESULT, REVISED:  rc=0, passed=2509, failed=0, per-target.
  2508 (bench/land-2655e3b_certified, head b17ec58) + 1.
  Falsifiers unchanged, with 2508 replaced by 2509 throughout.

-----------------------------------------------------------------------------------------------
AMENDMENT 2 — THE BASE MOVED 19 COMMITS. 2509 WAS RIGHT FOR MY OLD BASE AND IS WRONG FOR THIS ONE.
Written before the suite was run. No suite number has yet been taken on this branch: the first
launch was stopped while still queued, and the second ACQUIRED the lock and was then killed at
628 s (`REFUSING — aborted by SIGTERM. A suite cut short is not a count`), so it produced none.

⚠ **2508 AND 2509 ARE BASE-RELATIVE NUMBERS AND MUST NEVER BE QUOTED WITHOUT THEIR BASE.** 2508 is
`bench/land-2655e3b_certified`, head b17ec58 — which was `main` when this branch started. Since
then D138 landed `src/tel/tests_durable_log.rs` and three tests with it.

THE CURRENT BASE FIGURE, VERIFIED IN THIS TREE RATHER THAN TAKEN ON REPORT
  bench/land-ebdf002_certified/CERTIFICATION.txt:8 and runner.log:1 both read
    d138c: mode=per-target rc=0 passed=2511 failed=0 build_errors=0 head=5181d73
  `5181d73` resolves and `git merge-base --is-ancestor 5181d73 origin/main` is true, and
  `git diff --stat 5181d73..origin/main -- src tests examples Cargo.toml` is EMPTY — so no test
  appeared or vanished between the commit that count certifies and `origin/main` (4685d80).
  ⇒ origin/main is 2511 per-target.

THIS BRANCH'S DELTA, counted from what RUNS
  `git diff --name-status origin/main...HEAD -- tests/` lists exactly one added target,
  tests/d151_parse_reads_a_refreshed_snapshot.rs, and `cargo test --test
  d151_parse_reads_a_refreshed_snapshot` reports `1 passed; 0 failed`. One `#[test]`, no
  `macro_rules!` body, so the reaper_suite doubling trap does not apply.

PRE-REGISTERED RESULT, REVISED AGAIN:  rc=0, passed=2512, failed=0, per-target, on base 4685d80.
  2511 (origin/main, bench/land-ebdf002_certified, head 5181d73) + 1.
  Falsifiers unchanged, with 2512 substituted throughout.
  A `whole`-mode number is still a different quantity and may not be compared with this one.
