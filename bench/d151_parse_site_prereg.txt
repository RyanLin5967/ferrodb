PRE-REGISTRATION — d151-parse-site, written BEFORE its suite runs
==================================================================
Cut 2026-09-22T20:49Z on merge commit 2dd2c42 (d151-parse-site + main).

BASE FIXED AT: 68e24de (main), whose banked figure is 2529 per-target.

EXPECTATION: +0 @ 68e24de, absolute 2529 per-target.
  Derivation, so the claim can be checked rather than trusted: the branch adds NO test.
    git diff $(git merge-base main d151-parse-site) d151-parse-site -- tests/ \
      | grep -E '^\+' | grep -c '#\[test\]'   ->   0
  It changes one existing test body (a bounded NotLeader retry) and adds one bench file.
  ⚠ The ABSOLUTE 2529 is DERIVED from main's bank, not independently pre-registered. The
  +0 is the pre-registered quantity. Stated against my own favour.
  ⚠ MODE MATTERS: 2529 is per-target. `whole` mode would read 2531 (+2 doctests), and
  comparing across modes manufactures a phantom delta.

⛔ WHAT A GREEN RUN HERE DOES NOT MEAN. This is NOT a fix for the Windows
STATUS_ACCESS_VIOLATION and a green run is NOT evidence the AV is solved. The branch's own
commit 7fc1698 says so first, and bench/windows_access_violation.txt now records that the
AV is a LONG-STANDING INTERMITTENT across four distinct shas whose D138 bisect was
falsified (the three earlier crashes are git ancestors of the arm used as the clean
control). An intermittent passes most of the time.

⚠ THE MERGE WAS CLEAN AND THAT IS NOT SUFFICIENT. This branch and av-loop-counters both
edit tests/integration_cluster_agents.rs, so `a clean merge can assert both facts` applies:
rc=0 from git means no LINE collided, not that the result is coherent. Checked by hand
before this file was written, and recorded here:
    fn loopcount                     present at :1115
    loopcount(format_args!) sites    2
    d151's NotLeader retry           present at :1487
    bare eprintln! call sites        0   (the regression that would silence the instrument)

WHAT WOULD FALSIFY THE EXPECTATION
  Any count other than 2529 per-target at this merge commit. A LOWER count is a regression
  or a starved run and must be re-run, never reported as a count. A HIGHER count means a
  test was added that the derivation above says does not exist, and the derivation is then
  wrong and must be re-done rather than the number accepted.
