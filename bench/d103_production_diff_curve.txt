===============================================================================================
⛔⛔ D193 CORRECTION — 2026-09-23. THIS FILE'S LABEL IS FALSE. ITS NUMBERS ARE NOT.
The title ("the PRODUCTION DIFF path"), the Subject ("the call DIFF <branch> reaches"), the
Question ("does the production path cost ...") and the verdict ("the production path is
O(delta · log N)") all say the measured function is what a `DIFF <branch>` statement runs.
IT IS NOT, AND IT NEVER WAS. Read from source at fc9556a, by symbol:
  * dispatch::exec_agent, arm BoundAgentStmt::Diff  ->  AgentRuntime::diff, and nothing else.
  * AgentRuntime::diff builds the changeset from the workspace's own touched-rows map
    (ws.rows, ws.base_rows, ws.frame, state.applied) and descends no page tree. It calls
    neither page_changeset_with_cost, nor cow::diff::diff, nor CowTree::diff.
  * AgentRuntime::page_changeset_with_cost -- the SUBJECT of every number below -- has no
    caller in src/ outside page_changeset, which has none either. Its callers are integration
    tests and examples/d103_production_diff_curve.rs. `git log -S page_changeset -- src/`
    finds no commit that ever routed DIFF to it.
=> WHAT STANDS: the table is a real measurement of page_changeset_with_cost, the PAGE-DERIVED
   changeset: `visited` tracks depth (10 -> 14) while N grows 256x, and the CowTree::diff
   control tracks N. That is true of that function and of cow::diff's synchronised descent.
=> WHAT IS WITHDRAWN: every reading of this file as the cost of a DIFF statement. Read the
   verdict at the bottom as "page_changeset_with_cost is O(delta · log N)". What
   DIFF <branch> costs is AgentRuntime::diff's question, and nothing in this file measures it.
   Anything that quoted this file as "the production DIFF cost" inherits the error.
The false label was PRODUCED by the harness (its out.push_str lines), so correcting this file
alone would have been undone by the next run. The generator, its module doc, and the same false
sentence in src/cow/btree.rs, src/agent_sql/runtime.rs (DiffCost, page_changeset_with_cost) and
tests/integration_production_diff_wiring.rs are corrected in the same D193 commit that adds this
block. The file name is historical; renaming it would break its citations.
⛔⛔ END D193 CORRECTION — everything below is the original file, unedited.
===============================================================================================

d103 — the PRODUCTION DIFF path, cost curve (release)
run at unix 1790019730

Subject: AgentRuntime::page_changeset_with_cost, the call DIFF <branch> reaches.
Question: does the production path cost O(delta · log_m N) now, or O(N)?
delta is held at exactly 4 changed rows; N is the only axis.

Instruments:
  visited   DiffCost::visited — nodes the synchronised descent DECODED, which on
            that path is also every node it read. THE CLAIM.
  skipped   subtree pairs found equal by page identity and abandoned unread.
  walked    TreeDiff::pages_walked — walk_pages(base)+walk_pages(head), what the
            path this replaced must enumerate before it can prune. THE CONTROL.
  examined  TreeDiff::pages_examined — the old counter. It reports the DECODE
            half only, which is why an O(N) operation read as cheap.

Both paths were asserted to report the same 4 changes at every N; a row that did
not would have aborted the run rather than been banked.

      N   nodes  depth | visited  skipped  changes |     walked   examined
                       |                           |  (control)   (the lie)
-------  ------  ----- | -------  -------  ------- |  ---------   ---------
   1000      67      2 |      10       62        4 |        134          10
   4000     278      3 |      12       70        4 |        556          12
  16000    1151      3 |      12       82        4 |       2302          12
  64000    4607      3 |      12      130        4 |       9214          12
 256000   18234      4 |      14      141        4 |      36468          14

N grew 256x (1000 -> 256000).
visited grew 1.4x (10 -> 14), tracking DEPTH 2 -> 4.
control  grew 272x (134 -> 36468), tracking N.

The old counter, for contrast: examined 10 -> 14, which is flat and true and says
nothing at all about what that path cost.

PRE-REGISTERED OUTCOME 1: the production path is O(delta · log N).
