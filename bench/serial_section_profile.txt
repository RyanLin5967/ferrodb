S8: where the serial section goes. 20000 branches resident, 2000 iters each.
  operation                         ms/op     share
  core(parent)            lookup   0.00533     4.8%
  envelope_bytes(parent)  lookup   0.00693     6.3%
  FREE_ID first-key       scan     0.00715     6.5%
  write_record (3 upserts)         0.06316    57.3%
  child-key tree.insert (no del)   0.01003     9.1%
  write_header (1 upsert)          0.01756    15.9%
  publish_root                     0.00000     0.0%
  SUM                                0.11017
  (upsert)                           0.01344   <- one upsert alone, for scale

CORRECTED RUN. The previous version of this file priced the child key as an `upsert` at 0.0228 ms /
19.8%. `fork` calls `self.tree.insert` for that key directly, with no delete, so the row measured an
operation fork does not perform. Restated rather than carried forward:

  SUM 0.1148 -> 0.11017 ms.  Against the ~0.228 ms effective serial time under 64-thread
  contention, the seven operations are 48.3%, not "~50%". The old figure was an OVERESTIMATE.

⭐ THE COMPARISON THAT PRICES D10, AND IT IS APPLES TO APPLES -- same key space, same tree, same run:
      child key via upsert       0.02280 ms   (delete-then-insert)
      child key via tree.insert  0.01003 ms   (insert only)
      => THE WASTED DELETE IS 0.01277 ms, 56% OF AN UPSERT.
  On the fork path with a freshly minted id, the record, state, deadline and envelope keys are all
  provably new, so all four of those deletes are guaranteed misses. write_record's three upserts
  cost 0.06316 ms; at 56% that is ~0.035 ms recoverable, which against a 0.228 ms serial section is
  ~15% of fork throughput by D8's throughput = 1/serial_time. Larger than the ~11% first estimated,
  and the estimate is now a measurement of the delete rather than a guess at "roughly half".

⭐ IN-REPO PRECEDENT, WHICH IS ALSO THE SAFETY ARGUMENT: fork ALREADY uses tree.insert for the child
  key, eight lines above the code D10 changes. Somebody already reasoned that a fresh child key
  cannot exist and wrote the cheap call. D10 extends the identical reasoning to four more keys that
  are new for the identical reason.

THE SUBTRACTION STILL FAILS, AS EXPECTED. 4.093 - 3.612 = 0.480 ms "serial section", against a hard
bound of 0.228 ms from measured throughput. The harness prints its own warning for this. A flush
cannot be priced independently of the dirty set; the probe dirties one key, a fork dirties five
spans. The line is kept in the output precisely so nobody quotes it.
