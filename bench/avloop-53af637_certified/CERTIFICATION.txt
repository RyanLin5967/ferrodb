CERTIFICATION -- av-loop-counters landed into main
==================================================
landed:     53af637 (merge of cbf55d7)      base: 6b129b3 (via c0e5452)
delta:      +0 @ 6b129b3   absolute 2529 per-target   mode=per-target
gated sha:  cbf55d7        certify-head: exact match
go suite:   rc=0 passed=97 failed=0

PRE-REGISTERED, in db2aeed, BEFORE the run: "this adds NO test, so the expectation is
+0 @ 6b129b3, absolute 2529". Observed: 2529. The claim is in the commit, so it can be
read rather than recalled.

THE THREE GATES (tools/land-gate.sh, 2026-09-22T20:42:38Z)
  [1/3] certify-head  OK -- suite head=cbf55d7 == landing cbf55d7, rc=0 2529/0
  [2/3] staleness     COVERED-NO-CODE -- base moved 2 commits, NEITHER touching code:
                        bench/push_prereg_2026-09-22.txt
                        bench/windows_access_violation.txt
                      ⭐ Both were committed by me WHILE this suite ran. A disjoint file
                      set is NOT independence, which is why the guard was RUN and not
                      predicted -- and it answered with the precise form and named them.
  [3/3] prepush       OK -- FFI sweep; RUSTFLAGS="-D duplicate_macro_attributes -D dead_code"
                      build --examples and check --all-targets; windows-msvc cfg arm.
                      ⚠ The msvc check does not LINK, so it cannot see a missing POSIX symbol.

WHAT THIS LANDS, AND WHY THE PREVIOUS COMMIT COULD NOT HAVE WORKED
  4e50b0d instrumented both bounded loops with eprintln!. That macro routes through
  std::io::_eprint, which consults the thread-local output capture libtest installs.
  .github/workflows/tests.yml:306 runs a plain 'cargo test' with no --nocapture, so the
  line is emitted only for a test that fails in the ORDINARY way -- and the Windows
  access violation produces no 'test result: FAILED' and no 'panicked at', because the
  process dies before libtest writes a summary. Every captured buffer dies with it.

  ⛔ AND 4e50b0d DEFINED a missing line as "it died before either loop". So the broken
  instrument does not fail loudly; it renders as the most interesting possible finding.
  That is the detector failing in the direction that looks like a result.

FIRE-CHECK -- two arms, same command, no --nocapture, run before and after
  ARM A  eprintln!                       ->  0 LOOPCOUNT lines
  ARM B  direct write to io::stderr()    ->  2 LOOPCOUNT lines, with data
  and BOTH call sites proven to fire, not just one.
  Third confirmation: 16 LOOPCOUNT lines appear in THIS full verifier run, so the
  instrument works at suite scale under the real harness, not only in isolation.

macOS BASELINE -- the contrast the Windows reading is against (raw in loopcount_macos_baseline.txt)
  max turns = 3        bounds: settle_to 20000, hold_leader 100000
  max ms    = 10       16 loop exits
  Four orders of magnitude of headroom.

BLIND SPOTS
  * land-gate reads verdicts; it re-runs nothing and cannot tell you a run was honest.
  * This says nothing about whether the instrument will emit on WINDOWS specifically.
    The falsifier is pre-registered in bench/push_prereg_2026-09-22.txt: LOOPCOUNT lines
    on ubuntu but absent on Windows would void every absence reading, so the ubuntu log
    is checked FIRST.

certify-head.sh was run AGAINST THIS DIRECTORY at 2026-09-22T20:43:17Z and returned:
  certify-head: OK — suite head=cbf55d7 == landing cbf55d7
