PRE-REGISTRATION — D158 item 1, branch agent-acf966798a3979f57
Written BEFORE the suite run, against a base whose certified count is published.

BASE
  origin/main           4685d80
  certification         bench/land-ebdf002_certified/CERTIFICATION.txt
  certified per-target  2511    head=5181d73, rc=0, failed=0, build_errors=0
  go                    97      rc=0

  MODE MATTERS. 2511 is PER-TARGET. `whole` = per-target + 2 doctests, the second a
  compile_fail reported as its own block. A number quoted across modes is a phantom
  regression. This run is per-target.

  The 2508 in my own brief was STALE -- it is head=b17ec58, before D138 landed. Keeping
  it would have put this branch three above what its own additions explain.

WHAT THIS BRANCH ADDS -- counted from what RUNS
  tests/w4_stale_branch_crosses_agents.rs                                        +2
      a_reaped_sessions_select_must_not_read_the_new_occupants_staged_row
      a_reaped_sessions_abandon_must_not_retire_the_new_occupants_session
  src/agent_sql/runtime.rs, mod tests                                            +1
      the_reconciliation_crosses_its_chunk_boundary_and_forgets_every_branch
  ---------------------------------------------------------------------------------
  TOTAL                                                                          +3

  Both files are plain `#[test]` fns, not macro-instantiated, so a `grep -c` and what
  runs agree here. Checked rather than assumed: `cargo test --lib` reports 1658 on this
  branch against 1657 at the merge base, which is exactly the one lib test above.

PREDICTION
  per-target  passed = 2514   failed = 0   build_errors = 0
  go          97 (no Go source touched)

  Above 2514 means tests appeared that these three do not explain.
  Below 2514 means something that ran at 4685d80 stopped running.

FALSIFIERS -- any one withdraws the row rather than adjusting the number
  * the suite's head does not name the commit being landed            (certify-head)
  * the base moved under src/ while the suite ran, asked LAST         (staleness)
  * the fire-check's unmutated baseline had not passed first          (bench/w4_generation_keying/)

--- AMENDMENT 1, appended before the run. Nothing above is edited. ---

+1 test since the original count, for a defect the original change INTRODUCED and that I found
by reading my own diff rather than from a failure:

  src/agent_sql/runtime.rs, mod tests                                            +1
      a_recycled_slot_installs_the_new_workspace_before_releasing_the_old

  `insert_workspace` released the displaced workspace BEFORE installing the incoming one. The
  slot-keyed map got that order free from `BTreeMap::insert`, which installed and returned in
  one call; keying by `BranchId` split it in two and made it a decision. Commit 5c06685.

REVISED PREDICTION
  per-target  passed = 2515   failed = 0   build_errors = 0
  go          97
  = 2511 base + 2 integration (w4_stale_branch_crosses_agents) + 2 lib (chunk boundary, order)

  Checked, not assumed: `cargo test --lib` reports 1659 on this branch against 1657 at the
  merge base (4685d80 merged in at b675a95) -- exactly the two lib tests above.

FIRE-CHECK, run on the tree this prediction is about, baseline before any mutant was read:
  BASELINE unmutated                                       PASS
  evict          --lib                            KILLED
  evict_order    --lib                            KILLED
  chunk_stop     --lib                            KILLED
  overlay        w4_stale_branch_crosses_agents   KILLED
  record_read    w4_stale_branch_crosses_agents   KILLED
  blind_writes   w4_stale_branch_crosses_agents   KILLED
  seal           w4_stale_branch_crosses_agents   KILLED
  forget         w4_sweep_slot_recycle            KILLED

--- AMENDMENT 2, appended before any suite has completed. Nothing above is edited. ---

THE BASE MOVED WHILE THE FIRST RUN WAS IN FLIGHT, AND THE RUN WAS DISCARDED, NOT QUOTED.

`w4gen` acquired the suite lock at 16:08:48Z on base 4685d80 and was ~78/135 targets in when
`tools/staleness.sh . main HEAD` — which takes no lock and so is free to run mid-suite — read:

    branch agent-acf966798a3979f57 vs main: 14 behind, 11 ahead
      base moved under: 3 Rust file(s), 0 other code file(s)
    STALE-GREEN: the base moved under code this branch has not seen.
      tests present on main but NOT on this branch:
          tests/d151_parse_reads_a_refreshed_snapshot.rs

D151 had landed into LOCAL `main` (`3a9a5f9`); `origin/main` was 14 commits stale and still read
`4685d80`, which is why a fetch said "unmoved". The run was SIGTERMed; `verify-suite.sh` refused
with "A suite cut short is not a count" and released the lock. No partial count exists and none is
quoted. ⚠ The argument that the two file-sets are disjoint does NOT rescue a stale green, and the
guard says so in its own output: a lock-order inversion is created by two files that never appear
in the same diff, and it hangs rarely under load rather than failing loudly.

NEW BASE
  local main            3a9a5f9   (NOT origin/main, which is stale)
  certification         bench/d151_certified/CERTIFICATION.txt
  certified per-target  2512      head=cdf6434, rc=0, failed=0, build_errors=0
  go                    97        rc=0
  code identity         `git diff --stat cdf6434 main -- src tests examples Cargo.toml` -> EMPTY,
                        so main ships exactly the code that total certifies. Computed, not assumed.
  merged at             the merge commit below; staleness now reads COVERED, 0 behind.

REVISED PREDICTION
  per-target  passed = 2516   failed = 0   build_errors = 0
  go          97
  = 2512 base + 2 integration (w4_stale_branch_crosses_agents) + 2 lib (chunk boundary, order)

  My +4 is unchanged; only the base it lands on moved, 2511 -> 2512.

SIXTH CHECK, adopted from d154's lane: assert `tests/d151_parse_reads_a_refreshed_snapshot.rs`
appears BY NAME in the run. Its absence is exactly what staleness flagged, so a total one short
would mean D151's test did not come across in the merge — and a total alone cannot tell you that.
Confirmed present in the tree with 1 `#[test]`; `ls tests/*.rs` is 135, was 134.
