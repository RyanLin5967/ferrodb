⛔⛔ VERDICT 2026-09-23: **DESTROYED BY ITS ADVERSARY** (`frontier/d170_adversary.md`).
READ THIS BEFORE ANY NUMBER BELOW. The data are sound; the framing I was building on them was
wrong TWICE, and the headline result was already on disk before I measured it.

1. ⛔ **THE CAUSAL CLAIM IS FALSE.** This is NOT a stub-vs-arena comparison. It is a
   BEFORE-FIX vs AFTER-FIX comparison with the fix confounded in. `e6958d6` ("D71 FIX: push the
   predicate through the agent WRITE paths") landed **2026-09-20 14:14:44**, ONE HOUR after the
   stub run's instrument commit, and is an ancestor of HEAD.

2. ⛔ **THE ADVERSARY ISOLATED IT BY MEASUREMENT, NOT ARGUMENT.** Restoring only that call site
   (`D170_NOPUSHDOWN=1`), same binary, ARENA path:

       HEAD, pushdown ON        1.27x / 0.76x   ms/1000 FALLS
       NOPUSHDOWN (pre-fix)    11.78x / 14.55x  ms/1000 FLAT ~0.14-0.16
       stub run's headline      12.91x          ms/1000 FLAT ~0.17

   Pre-fix ARENA mean **13.17** brackets the stub's **12.91**. ⇒ **The stub defect moved D71's
   headline by approximately nothing.** The 2x2: across a row (no fix -> fix) 13-15x -> ~1x; down
   a column (stub -> arena) 12.91 -> 13.17 and 1.17 -> 1.02, i.e. NOTHING.

3. ⛔⛔ **AND THE RESULT WAS ALREADY BANKED THREE DAYS EARLIER — A RULE ZERO FAILURE OF MINE.**
   `bench/d71_pushdown_fix.txt` already holds the AFTER arm as FLAT: `AFTER 0.013 -> 0.016,
   ratio 1.23` (with 1.00 and 1.27 in sibling arms, mean 1.17), committed 2026-09-20.
   ⚠ **I OPENED THAT FILE EARLIER THE SAME SESSION AND READ ONLY ITS D101 BANNER** (`head -22`),
   stopping above the numbers. `bench/` is named in Rule Zero precisely because a measurement dies
   to PRIOR MEASUREMENT in your own `bench/`. Reading a file's header instead of its body is the
   same failure as reading `grep -c` instead of the matches, which I also did tonight.

✅ **WHAT SURVIVES, and it is small:** the **arena + fix** cell of the 2x2 was genuinely unmeasured
before today (`d71_pushdown_fix.txt`'s AFTER arm carries the D101 banner, i.e. it was measured on
the stub runtime). This run and the adversary's independently fill it: **1.02x (adversary), 0.81x /
0.69x (here)**. That is a REPLICATION plus one filled cell — **not a discovery**, and this file no
longer claims otherwise.

✅ **AND THE ONE THING WORTH THE PASS:** D71's ledger row still reads **OPEN** while its own exit
criterion — *"pass their predicate to `visible_rows_where` ... and the D71 staged curve goes FLAT"*
— has been MET since 2026-09-20 and is now measured three independent ways. The row is closed in
`SCALE-LEDGER.md` on the strength of that, not of this file.

===============================================================================================

D170 — RAW DATA ONLY. THE D71 RE-RUN THAT HAD NEVER HAPPENED.
⛔ INTERPRETATION IS DELIBERATELY ABSENT AND IS PENDING AN ADVERSARY (Rule One).
   Do not quote a conclusion from this file. It exists so the measurement survives the analysis.
   Prereg: bench/d170_prereg.txt (7f4f846), committed BEFORE the first number.

[HERE] 2026-09-23  examples/d71_point_update_curve.rs at HEAD (d1ddb04), UNMODIFIED, release.
Fleet lock `~/wt/logs/measure-lock.sh` held per block, released by a `trap ... EXIT` in the SAME
command as the acquire. ⚠ **THE BOX WAS NOT QUIET: load_at_acquire=26 (block 1), 20 (block 2).**
The lock script's own caveat applies: "Prefer COUNTERS (integers) over durations; if you must
report a duration, stamp load_at_acquire beside it and say it is an upper bound." **Every ms below
is an UPPER BOUND at that load. The `fsyncs/update` column is an integer and is load-immune.**

Why this run exists: `bench/d71_point_update_curve.txt` and `bench/d71_pushdown_fix.txt` both carry
a D101 CORRECTION — the harness built NO `AgentRuntime` and NO `ServerContext`, so the STAGED arm
timed an in-memory overlay, not the arena path `cli.rs` ships. `d71_pushdown_fix.txt`: "⛔ THE
RE-RUN HAS NOT HAPPENED. It was attempted and VOIDED — disk hit 0G ... eleven of its blocks exited
rc=127 while the runner printed 'ALL FIVE COMPLETE' over them." Verified before running: harness
fix `fc443c4` and guard `ca61e38` are both ancestors of HEAD.

=== BLOCK 1  (order: PLAIN then STAGED)  load_at_acquire=26  rc=0 both arms ===

  PLAIN                                          STAGED
  rows   med ms   ms/1000   fsync/upd            rows   med ms   ms/1000   fsync/upd
  1000    3.027    3.0267     1.03               1000    0.061    0.0609     0.00
  2000    3.048    1.5240     1.03               2000    0.053    0.0266     0.00
  4000    3.897    0.9743     1.00               4000    0.049    0.0122     0.00
  8000    4.013    0.5017     1.00               8000    0.050    0.0063     0.00
 16000    5.595    0.3497     1.00              16000    0.049    0.0031     0.00
  => 16x rows, 1.85x time                        => 16x rows, 0.81x time

=== BLOCK 2  (order: STAGED then PLAIN, counterbalanced)  load_at_acquire=20  rc=0 both arms ===

  PLAIN                                          STAGED
  rows   med ms   ms/1000   fsync/upd            rows   med ms   ms/1000   fsync/upd
  1000    3.017    3.0173     1.03               1000    0.074    0.0742     0.00
  2000    3.029    1.5144     1.03               2000    0.050    0.0250     0.00
  4000    3.803    0.9507     1.00               4000    0.049    0.0121     0.00
  8000    4.008    0.5009     1.00               8000    0.050    0.0063     0.00
 16000    4.992    0.3120     1.00              16000    0.051    0.0032     0.00
  => 16x rows, 1.65x time                        => 16x rows, 0.69x time

=== THE PRIOR (STUB) RUN, for comparison — bench/d71_point_update_curve.txt, first committed
=== cc6cbf6 2026-09-20, below its own D101 banner ===

  PLAIN                                          STAGED
  1000    4.032    4.0324     1.03               1000    0.185    0.1848     0.00
  2000    4.138    2.0688     1.03               2000    0.342    0.1708     0.00
  4000    4.093    1.0232     1.00               4000    0.688    0.1719     0.00
  8000    5.042    0.6303     1.00               8000    1.421    0.1776     0.00
 16000    5.396    0.3373     1.00              16000    2.385    0.1491     0.00
  => 16x rows, 1.34x time                        => 16x rows, **12.91x** time

=== CONTROLS, stated as facts about the data and nothing more ===

- `fsyncs/update`: **1.00-1.03 PLAIN, 0.00 STAGED, in ALL THREE runs.** Integer, load-immune,
  identical across the stub and both re-run blocks.
  ⚠ The prereg's outcome 5 called "not ~1 in either arm" an instrument failure. **That was written
  too bluntly and is not invoked**: a staged write does not commit, so 0.00 is correct behaviour on
  the staged path, not a broken instrument.
- **PLAIN is the arm the D101 defect provably could NOT touch** (ordinary DML never touches the
  runtime). Its ms/1000 at 16000 rows: stub **0.3373**, block 1 **0.3497**, block 2 **0.3120**.
- Block-to-block reproducibility within this re-run, PLAIN: 3.027/3.048/3.897/4.013/5.595 vs
  3.017/3.029/3.803/4.008/4.992.

=== WHAT IS NOT IN THIS FILE, ON PURPOSE ===

No conclusion about whether D71's headline stands, falls, or is unproven; no attribution of any
difference to any mechanism. An adversary is attacking the reading
(`frontier/d170_adversary.md`), including a specific alternative I raised against myself: the
overlay probe (`overlay_probe_key`) landed `65287c2` **2026-09-18**, BEFORE the stub run was
committed `cc6cbf6` **2026-09-20**, so the probe cannot be the explanation. ⚠ And the D101
correction predicts the OPPOSITE of what is above, in its own words: "the overlay walk in
`visible_rows_where` ... runs in both configurations. **The COMPLEXITY CLASS may therefore hold**
while every absolute here ... do not."

⛔⛔ **APPENDIX A'S CONCLUSION IS RETRACTED IN FULL, 2026-09-23. THE RAW NUMBERS STAND; WHAT I
CONCLUDED FROM THEM WAS WRONG THREE WAYS.** Retracted here, where the claim was written.

1. ⛔ **I CITED D167 INVERTED.** I wrote "the staged path is O(staged rows)". `d167_prereg.txt:33-35`
   says the opposite: *"**demoted** (`D56_DEMOTE=1`): **the probe is disabled**, so
   `visible_rows_where` walks the whole table prefix of the overlay — **O(W) per read** … The
   discriminator is the SLOPE of the **demoted** arm against a **flat control**."* **O(W) is the
   DEMOTED arm; the probeable state is the FLAT CONTROL.** Demotion requires a PK-MOVING update
   (`d167_prereg.txt:15`, `UPDATE t SET id = …`), which increments `unprobeable_rows`
   (`runtime.rs:3057`). **D71 writes `SET v = {i}` and never moves the PK**, so
   `unprobeable_rows == 0`, the probe fires, and **the overlay here is O(1)** — D167's flat control
   regime, not its demoted arm.

2. ⛔ **MY PREDICTION WAS MISCAST AS A BINARY AND THE DATA FIT NEITHER BRANCH.** I wrote
   "rises ⇒ walk live; flat ⇒ probe-served". The rise is **SUBLINEAR**: the adversary's sweep at a
   fixed 4000-row table gives 50.25 → 161.75 us/update for **64x** the staged rows (**3.2x**), and
   the pre-fix arm 600.63 → 785.21 (**1.31x**). Sublinear in both states, **linear in neither**. A
   sublinear rise is not evidence of the O(W) walk, and my two-branch prediction had no cell for it.

3. ⛔ **AND THE HYPOTHESIS WAS ALREADY DEAD IN THE STUB RUN'S OWN FILE**, which I had open:
   *"64x the session size costs 1.45x per update. THE HYPOTHESIS IS DEAD."* Third time tonight that
   I re-derived something sitting in a file I had already opened.

⇒ ✅ **WHAT THE APPENDIX-A NUMBERS ACTUALLY SHOW:** a sublinear rise with staged rows, consistent
with the stub run's own N-sweep, and **not** an O(staged-rows) term. The claim that the overlay is
the residual is withdrawn. The real residual question — why the probe does or does not serve — is
answered: it **does** serve here, by construction.

===============================================================================================

=== APPENDIX A — THE AXIS TEST. RAW. Prediction stated BEFORE the run, quoted verbatim. ===

Run 2026-09-23, same binary, fleet lock held with trap-release, **load_at_acquire=17 — NOT a quiet
box, every ms below is an UPPER BOUND.**

Prediction, printed before the first number:
  "Hold TABLE ROWS fixed at 4000; vary D71_N (= rows staged during the run).
   * If the overlay WALK is live: median ms RISES with N. D167 measured 13-17 ns/staged row, so at
     N=4000 (~2000 staged at the median) expect ~+30 us against a ~50 us baseline -- visible.
   * If the PROBE fires (pk = literal takes staged.get, O(1)): median ms FLAT in N.
   FLAT in N => the staged path is probe-served and the overlay walk is NOT the residual term."

  D71_ARM=staged  D71_SIZES=4000  (table rows FIXED)
    N=60     med 0.051 ms    ms/1000rows 0.0128    fsync/upd 0.00
    N=250    med 0.059 ms    ms/1000rows 0.0147    fsync/upd 0.00
    N=1000   med 0.083 ms    ms/1000rows 0.0208    fsync/upd 0.00
    N=4000   med 0.190 ms    ms/1000rows 0.0476    fsync/upd 0.00

RESULT: **RISES.** Not flat. The "probe-served" branch of the prediction did NOT fire.

Arithmetic, stated as arithmetic: the median update sees ~N/2 staged rows, so N=60 -> ~30 and
N=4000 -> ~2000. Δ = 0.190 - 0.051 = 0.139 ms over ~1970 staged rows ≈ **70 ns per staged row**.
⚠ **D167 measured 13-17 ns/staged row. This is 4-5x higher and the two are NOT claimed to agree** —
different harness, different method (D167 timed an EXCESS over a control), and load 17 here. The
SHAPE (rises with staged rows) is the result; the rate is not.

WHAT THIS ESTABLISHES, AND ONLY THIS:
- The staged path's residual cost is **O(staged rows)**, measured, on this harness at HEAD.
- It is therefore **NOT O(table rows)** — which is why the main curve above is flat in table rows.
- ⚠ The probe (`overlay_probe_key`, fires only when `unprobeable_rows == 0`) is evidently **not
  serving these updates**, or not always. WHY is UNKNOWN and is not investigated here.

⛔ STILL NOT A CONCLUSION ABOUT D71. Two things changed between the stub run and this one and the
ordering is exact: `cc6cbf6` stub artifact **2026-09-20 13:21:52**, `e6958d6` "D71 FIX: push the
predicate through the agent WRITE paths" **2026-09-20 14:14:44** (53 minutes later), `fc443c4`
harness correction **2026-09-21 01:10:50**. Attribution is the adversary's question
(`frontier/d170_adversary.md`), not settled here.
