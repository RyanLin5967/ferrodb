CERTIFICATION -- D167 landed into main
======================================
landed:    883834b (merge of 444ad6a)      base: 0de43e7
delta:     +0 @ 0de43e7   absolute 2529 per-target   mode=per-target
gated sha: 444ad6a        certify-head: exact match
go suite:  rc=0 passed=97 failed=0

PRE-REGISTERED in bench/d167_landing_prereg.txt BEFORE the run, with the derivation printed in
the file itself (git diff --name-only shows no tests/ file is touched, so the count cannot move).
The +0 is the pre-registered quantity; the absolute 2529 is DERIVED from main's bank and the
prereg says so against its own favour.

THE THREE GATES (tools/land-gate.sh, 2026-09-23T00:30:12Z)
  [1/3] certify-head  OK -- suite head=444ad6a == landing 444ad6a, rc=0 2529/0
  [2/3] staleness     COVERED
  [3/3] prepush       OK -- FFI sweep; CI RUSTFLAGS build --examples and check --all-targets;
                      windows-msvc cfg arm. ⚠ The msvc check does NOT link.

⭐ THE LOCK WAS RELEASED BY A TRAP IN THE SAME COMMAND THAT DID THE WORK.
  Two hours earlier I held this lock for 2h23m across a stall: the measurement finished at
  21:47:59Z and the release was queued behind a turn boundary that a usage limit landed in.
  This landing used `trap 'rm -rf /tmp/ferrodb-suite.lock' EXIT`, so the lock could not outlive
  the command whatever happened to the session. See FLEET.md.

WHAT THIS LANDS, AND WHAT IT IS NOT
  A D56_DEMOTE knob on examples/d56_plan_curve.rs: one PK-moving UPDATE makes unprobeable_rows
  non-zero, so visible_rows_where WALKS instead of probing. The two arms then differ in exactly
  one thing -- whether the probe is enabled -- and the moved key is outside the table and is not
  the key under test, so the read still comes from the base table.

  RESULT: control FLAT at ~7.7us/stmt across a 32x range of W; demoted excess LINEAR at
  ~13-17 ns per staged row. The positive control was fired BEFORE the sweep, as the
  pre-registration required (W=800: control 123349, demoted 50021 stmt/s).

⛔ IT IS A REPLICATION AND THE ARTIFACT SAYS SO IN ITS OWN HEADER.
  bench/d57_staged_curve_before.txt / _after.txt measured this curve three days earlier with the
  same harness and the same axis -- and with BETTER method: "Order: forward, reverse, forward
  (point-rotation)". This run did NOT rotate; it ran control-then-demoted at every W, so drift is
  not cancelled between arms. Rule Zero (grep bench/ before building) would have caught this and
  I did not run it on my own measurement.

⛔ IT IS NOT A BUG REPORT. The monotone, inherited, never-decremented counter is DOCUMENTED INTENT
  (runtime.rs:340-344: "Monotone and conservative ... inherited by a forked child ... never
  decremented") and deliberately TESTED (tests/d57_overlay_probe.rs:346).

DELIBERATELY UNEXPLAINED, so nobody quotes a mechanism that was never checked
  * the ns/row figure drifts 13.0 -> 16.6 across the sweep; load was 13.61 and cache behaviour is
    plausible, but this project's rule is that a mechanism for a gap must itself be checked first.
  * this exceeds d57's published ~11.5 ns/row while measuring a NARROWER path. Not comparable:
    runtime.rs:2120 scopes that figure to "all tables" under the State mutex; this is one table's
    prefix on a snapshot taken outside the lock.

BLIND SPOTS
  * land-gate reads verdicts; it re-runs nothing and cannot tell you a run was honest.
  * No arm rotation, as above. Treat the two arms as adjacent-in-time, not balanced.

certify-head.sh was run AGAINST THIS DIRECTORY at 2026-09-23T00:30:45Z: OK, head=444ad6a == 444ad6a.
