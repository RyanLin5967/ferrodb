CERTIFICATION — d127-invert (the D134 prose inversion), 2026-09-22
================================================================================
⚠ SUPERSEDES SUMMARY.txt in this directory, which certifies head=7ad789b in
  mode=whole. That run is NOT about the tree that ships: it predates both the
  prose inversion and 48 commits of main. Read this file, not that one.

suite head=  3b0bee5   (branch d127-invert)
mode=        per-target
  ⚠ MODE IS PART OF THE NUMBER. This 2482 is NOT comparable term-for-term with
    the superseded 2467 in SUMMARY.txt.
  ⛔ CORRECTED 2026-09-22: this line said `whole` = `per-target` + 2 DOCTESTS.
    THAT IS FALSE, and it is contradicted by suite_tail.log IN THIS DIRECTORY,
    which shows "Doc-tests ferrodb / running 1 test". So does d126_certified's
    log, and so does d141merged's. THIS CRATE HAS EXACTLY ONE DOCTEST
    (`src/cow/diff.rs - cow::diff::MemoIdentity`, line 443).
    ⇒ The +2 PROPAGATED: D141 and the lead both carried it, and it is exactly
      the size of a still-open 1-test gap between a whole-mode 2492 and a
      per-target 2482. A wrong constant of the RIGHT MAGNITUDE is the hardest
      kind to catch, because applying it CLOSES the arithmetic and ends the
      investigation -- which is what it nearly did.
    ⇒ The true relationship AT THIS COMMIT is `whole` = `per-target` + 1, and
      even that is a measurement, not a law. Re-read the Doc-tests line rather
      than carrying a remembered constant.

pre-registered: 2482, in bench/d127_invert_prereg.txt as AMENDMENT 1, cut
  2026-09-22T12:11:43Z while the run was still in flight and no verdict existed
  on disk. The original figure in that file was 2481 and was WRONG; it is left
  in place above the amendment rather than edited, so the correction is visible.

Result (verbatim from the runner's own stdout):
d127invert: mode=per-target rc=0 passed=2482 failed=0 build_errors=0 head=3b0bee5 log=/Users/idide/wt/artie-research/verify-out/d127invert/suite-d127invert.log
d127invert: go rc=0 passed=97 failed=0 log=/Users/idide/wt/artie-research/verify-out/d127invert/suite-d127invert.go.log
HARNESS_EXIT=0

WHAT THIS GREEN DOES AND DOES NOT LICENSE
  DOES: it is evidence about this branch's change — which is comments, doc
    comments and ONE operator-facing string literal, and nothing else. The only
    non-comment line in `git show 3b0bee5 -- src/` is that string.
  DOES NOT: license a push. It certifies a tree that merged main at 29ad1f5.
    main has since moved to 955e992 (D130-batch-vs-threads and d85-rederive
    landed after this branch last synced), so `main + this branch` is a tree no
    suite has seen. Merging and pushing on the strength of THIS green would be
    the D126 failure exactly. The load-bearing run is the one over the merged
    tree, and it is the team lead's to make.

BLIND SPOTS, per this repo's convention that evidence names its own
  * A commit added after 3b0bee5 is not covered. One exists: the commit carrying
    this file and the prereg amendment. It is bench/*.txt only — zero src/, zero
    tests — so it cannot move a test count, but it is not what ran.
  * D139's fixture race (PID-keyed temp paths in reaper.rs) passed in this run.
    That is one draw on an intermittent, not a retraction of it.
  * This run never compiled examples/d130_pgwire_batch.rs, which arrived on main
    after this branch synced. verify-suite.sh builds examples before it tests, so
    the first run to compile that example alongside this branch is the merged one.
